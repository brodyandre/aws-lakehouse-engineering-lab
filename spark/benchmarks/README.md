# Benchmarks Spark

Este diretório foi reservado para experimentos comparativos de performance.

Exemplos de benchmark planejados:

- comparação entre CSV e Parquet;
- impacto de particionamento por data;
- variação de `shuffle partitions`;
- comparação entre joins com e sem broadcast;
- tempo de escrita por camada.
